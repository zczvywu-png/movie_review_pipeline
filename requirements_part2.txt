# Part-2 only — append on top of requirements.txt.
# These are needed by the Part-1 → Part-2 bridge that builds clusters.json.
# OpenSCAD itself is a desktop application; install separately from
# https://openscad.org/downloads.html (no Python package required).
# Blender 4.x ships with its own Python; the blender/*.py scripts only
# import bpy + mathutils which are bundled.

pillow>=10.0,<12.0           # poster mean-RGB extraction in bridge/export_clusters_json.py
